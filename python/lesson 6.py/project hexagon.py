import turtle

turtle.Screen().bgcolor("purple")

sc=turtle.Screen()
sc.setup(400 , 300)

board=turtle.Turtle()

length=50

board.forward(length)
board.left(60)
board.forward(length)
board.left(60)
board.forward(length)
board.left(60)
board.forward(length)
board.left(60)
board.forward(length)
board.left(60)
board.forward(length)